name: 2nd worflow
on: [push]

jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repo
              uses: actions/checkout@v5
            
              run: |
                chmod +x shell.sh
                ./shell.sh